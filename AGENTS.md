# Instructions for AI agents

- Always run `npm run lint` after completing any task.
- Fix all lint errors before marking the task as done.
- If the linter has auto-fix (`--fix`), run it first: `npm run lint -- --fix`
